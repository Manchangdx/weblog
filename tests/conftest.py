# 终端执行 pytest 命令即可进行测试，可以加上 -s 参数显示打印信息
# 被测试的文件必须以 test_ 开头
# 被测试文件中的类必须以 Test 开头，文件中的函数和类中的方法必须以 test 开头

# 测试的最小单位是函数和方法，以下简称「函数」
# 被测试文件中的函数在执行时，所需参数都从这里调用
# 每测试一个函数都要单独获取所需参数，函数之间没有关联

from .fixtures import *
